# Local compose (docker-compose.yml)
STUDYHUB_HTTP_PORT=9080
STUDYHUB_PUBLIC_URL=http://localhost:9080

# GHCR pull compose (docker-compose.ghcr.yml) — use your GitHub org/repo, lower case
# GHCR_IMAGE_PREFIX=ghcr.io/yourgithubuser/studyhub
# IMAGE_TAG=latest
